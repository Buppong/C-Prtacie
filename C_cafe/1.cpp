#include <stdio.h>

int main() {

	int a, b;

	printf("덧셈할 수를 입력하세요:");
	//두개의정수값을 입력받는다.
	scanf_s("%d %d", &a, &b);
	//입력ㅂ다은 정숫값과 합산된 결과를 출력한다.
	printf("%d + %d = %d\n", a, b, a + b);

	return;
}


int main() {

	int a, b;

	printf("덧셈할 수를 입력하세요:");
	//두개의정수값을 입력받는다.
	scanf_s("%d %d", &a, &b);
	//입력ㅂ다은 정숫값과 합산된 결과를 출력한다.
	printf("%d + %d = %d\n", a, b, a + b);
	printf("덧셈할 수를 입력하세요:");
	//두개의정수값을 입력받는다.
	scanf_s("%d %d", &a, &b);
	//입력ㅂ다은 정숫값과 합산된 결과를 출력한다.
	printf("%d + %d = %d\n", a, b, a + b);
	printf("덧셈할 수를 입력하세요:");
	//두개의정수값을 입력받는다.
	scanf_s("%d %d", &a, &b);
	//입력ㅂ다은 정숫값과 합산된 결과를 출력한다.
	printf("%d + %d = %d\n", a, b, a + b);

	return;
}


//예를들어 코드가 중복